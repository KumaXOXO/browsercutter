server { server_name browsercutter.xyz www.browsercutter.xyz; root /var/www/browsercutter; index index.html; # Required for ffmpeg.wasm SharedArrayBuffer support add_header Cross-Origin-Opener-Policy "same-origin"; add_header Cross-Origin-Embedder-Policy "require-corp"; location / { try_files $uri $uri/ /index.html; } # Cache static assets location ~* \.(js|css|wasm|png|ico|svg)$ { expires 1y; add_header Cache-Control "public, immutable"; add_header Cross-Origin-Opener-Policy "same-origin"; add_header Cross-Origin-Embedder-Policy "require-corp"; } listen 443 ssl; # managed by Certbot ssl_certificate /etc/letsencrypt/live/browsercutter.xyz/fullchain.pem; # managed by Certbot ssl_certificate_key /etc/letsencrypt/live/browsercutter.xyz/privkey.pem; # managed by Certbot include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot } server { if ($host = www.garnixaufenglisch.de) { return 301 https://$host$request_uri; } # managed by Certbot if ($host = garnixaufenglisch.de) { return 301 https://$host$request_uri; } # managed by Certbot listen 80; server_name garnixaufenglisch.de www.garnixaufenglisch.de; return 404; # managed by Certbot }